name: coding-interview-book
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - jupyterlab
  - numpy
  - matplotlib
  - pandas
  # For code examples in the book
  - ipywidgets
  - pip
  - pip:
    - mystmd  # MyST v2 for building the book
    - ghp-import  # For deploying to GitHub Pages
